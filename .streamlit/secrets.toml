CLIENT_ID = "someid"
CLIENT_SECRET = "somesecret"

[admin_users]
"admin@example.com" = "changeme"

# ---------------------------------------------------------------------------
# OOTB (out-of-the-box) setups for the "Deploy new OOTB Setup" page.
# Source (template) credentials are the CLIENT_ID / CLIENT_SECRET above.
# All ids below are TEMPLATE-company ids:
#   project_ids             — the projects that make up the setup
#   enrichment_workflow_ids — the composite enrichment workflow(s) to clone
#   agent_workflow_ids      — the agentic (dynamic) workflow(s) to clone
# The enrichment / agent workflow ids are required because template workflows
# are not necessarily bound to these projects, so they can't be auto-discovered.
# ---------------------------------------------------------------------------
[ootb]
source_region = "EU"   # "EU" or "US" — region the template company lives in

[ootb.setups.invoice_processing_a]
label                   = "Invoice Processing — Setup A"
group                   = "Invoice Processing"
project_ids             = []   # e.g. ["<template-project-id-1>", "<template-project-id-2>"]
enrichment_workflow_ids = []   # e.g. ["<enrichment-workflow-id>"]
agent_workflow_ids      = []   # e.g. ["<agent-workflow-id>"]

[ootb.setups.invoice_processing_b]
label                   = "Invoice Processing — Setup B"
group                   = "Invoice Processing"
project_ids             = []
enrichment_workflow_ids = []
agent_workflow_ids      = []

[ootb.setups.order_confirmation]
label                   = "Order Confirmation"
group                   = "Order Confirmation"
project_ids             = []
enrichment_workflow_ids = []
agent_workflow_ids      = []

[ootb.setups.order_management]
label                   = "Order Management"
group                   = "Order Management"
project_ids             = []
enrichment_workflow_ids = []
agent_workflow_ids      = []
